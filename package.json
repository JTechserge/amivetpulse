{
  "name": "calendrier-amivet",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Amivet PULSE — PWA planning vétérinaire",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint src/ --max-warnings=0",
    "format": "prettier --write src/",
    "test:unit": "vitest run",
    "test": "playwright test",
    "test:install": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.48.0",
    "eslint": "^10.7.0",
    "eslint-plugin-no-unsanitized": "^4.1.5",
    "globals": "^17.7.0",
    "http-server": "^14.1.1",
    "prettier": "^3.9.5",
    "vite": "^8.1.3",
    "vitest": "^4.1.10"
  }
}
